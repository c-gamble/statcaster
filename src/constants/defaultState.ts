export const DEFAULT_STATE = {
    chain: process.env.NEXT_PUBLIC_DEFAULT_CHAIN,
    tokenAddress: process.env.NEXT_PUBLIC_DEFAULT_TOKEN_ADDRESS,
    gradientStart: process.env.NEXT_PUBLIC_DEFAULT_GRADIENT_START,
    gradientEnd: process.env.NEXT_PUBLIC_DEFAULT_GRADIENT_END,
    holderCount: true,
    // dailyVolume: true,
    // monthlyVolume: true,
    totalSupply: true,
    centralization: true,
};
